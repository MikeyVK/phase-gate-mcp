<!-- .st3\temp\pr_render.md -->
<!-- template=pr version=93bb9b4e created=2026-05-03T20:51Z updated= -->
# feat: run_tests — expose actionable failure details in tool response (#300)

run_tests now returns actionable per-failure detail directly in the tool response, eliminating the need for agents to fall back to run_in_terminal for test diagnosis.
## Changes
**mcp_server/tools/test_tools.py:** RunTestsInput gains `coverage: bool = False`; `_to_tool_result()` routes by exit code — exit 1 adds `FAILED test_id — short_reason` lines to content[0]; exits 2/3/4 add stderr hint and set is_error=True.

**mcp_server/managers/pytest_runner.py:** ExitCodePolicy.outcome extended to Literal['ok','error','raise']; PytestResult gains `is_error: bool` + `stderr: str`; `_extract_traceback()` regex handles pytest-xdist [gwN] prefix.

**content[1] JSON — new fields:** exit_code, coverage_pct, lf_cache_was_empty, stderr (always present).

**Documentation:** full authoritative run_tests contract section in quality.md; coverage param and new contract in MCP_TOOLS.md, tools/README.md, TOOLS.md, agent.md.

## Testing
4 TDD cycles (C1–C4), 44 tests passing. Quality gates: all pass (branch scope). QA doc review: GO after 3 corrections.
## Checklist

- [ ] Code follows project standards
- [ ] Tests added/updated
- [ ] Documentation updated
- [ ] Quality gates passing
## Related Documentation
- **[docs/reference/mcp/tools/quality.md][related-1]**
- **[docs/reference/mcp/MCP_TOOLS.md][related-2]**

---

Closes: #300